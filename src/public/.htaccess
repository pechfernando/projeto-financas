RewriteEngine On

# Qualquer requisição para /api/algo é redirecionada para /api/index.php,
# que decide o que fazer a partir da URL (veja app/Api/Router.php)
RewriteCond %{REQUEST_URI} ^/api/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ /api/index.php [L]
